
    - 1️⃣ What is the difference between var, let, and const?
    ans: In generally let, const and has been hoisted but but let and const has TDZ (Temporal Dead Zone) which is prevent unexpected bugs.
    let $ var value can reassign but const cant.
    - 2️⃣ What is the spread operator (...)?
    ans: Spread operator is a function which is use for expand element in an array or object.
    - 3️⃣ What is the difference between map(), filter(), and forEach()?
    map(), filter(), and forEach are array power methods in JavaScript. All of them loop through an array, but they have different purposes. map() returns a new array and modifies each element. filter() selects elements based on a condition and creates a new array. forEach() only loops through the array and does not return anything. 
    - 4️⃣ What is an arrow function?
    ans: An arrow function is a shorter syntax for writing functions in JavaScript. It was introduced in ES6.
    - 5️⃣ What are template literals?
    ans: Template literals are a way to write strings in JavaScript using backticks ( ). They allow embedding variables and expressions using ${}.


